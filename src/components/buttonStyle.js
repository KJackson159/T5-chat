const btstyle = {
  disableButton: {
    backgroundColor: "gray",
    cursor: "not-allowed",
  },
  enableButton: {
    backgroundColor: "orange",
    cursor: "pointer",
  },
  encryptOn: {
    backgroundColor: "red",
    color: "white",
    cursor: "pointer",
  },
  encryptOff: {
    backgroundColor: "gray",
    color: "black",
    cursor: "pointer",
  },
};

export default btstyle;
